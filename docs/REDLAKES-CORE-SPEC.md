# REDLAKES CORE — Spécification fonctionnelle

> Document de référence validé pour la transformation progressive du site REDLAKES RP existant en REDLAKES CORE : le site (Web + API + PostgreSQL) devient la source de vérité unique pour l'écosystème ; Discord, Minecraft et les futures IA en deviennent des clients.
>
> Règle absolue : **CONSERVER → ADAPTER → ENRICHIR → REMPLACER UNIQUEMENT SI NÉCESSAIRE**. Ne jamais reconstruire une fonctionnalité qui fonctionne déjà, ne jamais refaire la page d'accueil, la direction artistique ou la navigation. Toujours enrichir l'existant, jamais repartir de zéro.

## Objectif

Créer le véritable centre de contrôle de REDLAKES : le CORE décide, les autres systèmes (Discord, Minecraft, IA) appliquent.

## Ce qui est conservé intégralement

- Page d'accueil, DA, animations, navigation, footer — inchangés.
- Toutes les pages publiques (Wiki SCP, Lore, Personnages, Factions, MTF, Départements, Site-12, Carte, Chronologie, Actualités, Galerie, CASSIE, Transmissions, Joueurs, Connexion) — design identique.
- CMS actuel (édition, création, publication, historique, recherche) — devient la base du futur système documentaire.
- Authentification (Discord OAuth, Dev Login, sessions, cookies, sécurité).
- Dashboard joueur (identité RP, rapports, candidatures, statistiques, Discord) — enrichi, pas refait.
- Dashboard staff (candidatures, rapports, audit, commentaires, historique) — enrichi, pas refait.
- Bot Discord — conservé entièrement, devient progressivement un client du CORE.
- API NestJS / Prisma / PostgreSQL — architecture, services, modules, guards conservés.

## Ce qui est enrichi

### Système des joueurs
Identité, matricule, nom/prénom RP, Discord, Minecraft, grade, faction, département, équipe, clearance, salaire, solde, statistiques, réputation, médailles, historique (grades/factions/économie/activités), missions, rapports, candidatures, notifications, sanctions.

### Système des grades — la source de vérité centrale
Un `Grade` définit : nom, description, faction, département, tier, salaire, clearance, permissions Web/Discord/Minecraft, zones accessibles, documents accessibles, hiérarchie. Modifier un grade se propage automatiquement au site, à Discord et à Minecraft.

### Factions
Nom, description, logo, couleur, historique, chef, sous-chefs, départements, équipes, membres, budget, documents, missions, relations diplomatiques.

### Départements
Chef, adjoints, équipes, membres, budget, documents, missions (ex. Recherche, Sécurité, Administration, Maintenance, Médical, Logistique, Commandement).

### Documents classifiés
Titre, slug, auteur, classification, statut, version, historique, pièces jointes, tags, personnes/factions/SCP/événements/missions/documents liés. Clearance **toujours vérifiée côté API**, jamais uniquement en React.

### Missions
Titre, description, objectifs, difficulté, statut, créateur, participants, récompenses, expiration, validation, historique, événement lié, rapports liés. Individuelles, d'équipe ou de faction.

### Événements RP
Statut, gravité, conséquences, participants, missions générées, historique, notifications, journal (brèche SCP, attaque, évasion, guerre, incident, catastrophe, alerte...).

### Économie
Wallet, transactions (salaire, prime, amende, transfert), historique, comptes de faction/département. Transactions **immuables**, opérations PostgreSQL atomiques.

### Sanctions
Auteur, raison, début, fin, type, commentaires, historique, documents liés.

### Permissions
Suppression **progressive** (pas immédiate) de la logique actuelle basée sur `PLAYER/STAFF/ADMIN` au profit de permissions dérivées du Grade. Le système de rôles actuel reste en place en parallèle jusqu'à preuve que les permissions dérivées du Grade donnent les mêmes résultats.

## Base de données — nouveaux modèles prévus

`Grade` (✅ créé), `Faction`, `Department`, `Team`, `Wallet`, `Transaction`, `Mission`, `MissionParticipant`, `RpEvent`, `RpEventParticipant`, `Sanction`, `GradeHistory`, `FactionHistory`, `ClassifiedDocument`, `DocumentVersion`, `DocumentAttachment`.

## Migration

Ne jamais supprimer les données existantes (Lore, Wiki, SCP, Actualités, Chronologie, CMS, Rapports, Candidatures). Migration progressive, un lot à la fois, chaque lot vérifié (build/lint/tsc + test contre une vraie base) avant de passer au suivant.

## Discord / Minecraft / IA

- **Discord** : devient un client du CORE (grades, missions, événements, documents, notifications, profils).
- **Minecraft** : rien développé pour l'instant. Le CORE prépare l'architecture ; le plugin (`redlakes-sync`, à réécrire pour Spigot/PaperSpigot `1.12.2-R0.1-SNAPSHOT` + Java 8, jamais Paper 1.21/Java 21) sera développé plus tard. Minecraft appliquera les données du CORE, ne sera plus jamais la source de vérité du grade.
- **IA** : architecture préparée (configuration, permissions, feature flags, connecteurs futurs) — aucune IA développée maintenant. CASSIE reste inchangée.

## Sécurité

Toutes les vérifications côté API. Toutes les opérations économiques atomiques. Toutes les modifications journalisées (`AuditLog`). Toutes les permissions centralisées. Toutes les routes sensibles protégées.

## Historique des lots livrés

| Lot | Contenu | Statut |
|---|---|---|
| 1 | Modèle `Grade` (lecture seule), seed depuis `rp-grades.ts`, `GET /grades` | ✅ Livré et vérifié |
| 2 | `Player.gradeId` → relation `Grade`, résolution du grade dans `sync.service.ts` (catalogue en priorité, ancienne table floue en repli), backfill des joueurs existants, `gradeInfo` exposé dans les réponses `/players/*` | ✅ Livré et vérifié |
| 3 | Modèles `Faction`/`Department` (lecture seule), seed depuis `factions.ts`/`site12.ts`, `GET /factions(/:slug)`, `GET /departments(/:slug)`, relation Faction↔Department | ✅ Livré et vérifié |
| 4 | `Player.factionId` → relation `Faction`, `Grade.departmentRefId` → relation `Department` (résolu depuis le slug déjà présent dans `Grade.departmentId`), `sync.service.ts` résout la faction en plus du grade, backfills | ✅ Livré et vérifié — **gap identifié** : 14/85 grades (Oméga, Direction, Détention, "Scientifique") ne matchent aucun `Department` — ces catégories n'ont jamais été modélisées comme départements (seuls les 4 de `site12.ts` existent), décision à prendre |
| 5 | Premières pages web publiques consommant le catalogue CORE : `/grades` (catalogue filtrable, groupé par branche) et `/grades/[slug]` (fiche détail — clearance, salaire, quota, zones d'accès, sections du site, lien vers le département). Aperçu "Conseil Oméga" ajouté sur la page d'accueil (données live, section masquée si l'API est indisponible). Aucune page/route existante modifiée, hors une entrée de menu et une nouvelle section d'accueil | ✅ Livré et vérifié |
| 6 | Le bot Discord devient un vrai client CORE pour les grades : `/grades` lit désormais `GET /grades` en direct au lieu d'un miroir statique déjà obsolète (`lib/site12.ts`, supprimé) ; `/profil` affiche département et salaire via `gradeInfo`/`factionInfo`, désormais exposés par `getProfileByDiscordId` | ✅ Livré et vérifié |
| 7 | CMS Staff pour les Grades (`/staff/grades`, `/staff/grades/nouveau`, `/staff/grades/[id]`) : CRUD complet côté API (`POST/PATCH/DELETE /grades`, `GET /grades/by-id/:id` staff), formulaire d'édition complet côté site. Réponse directe à la demande de pouvoir éditer le site depuis le site. A aussi révélé et corrigé une dépendance circulaire de modules Nest (`Auth → Sync → Grades → Auth`, résolue avec `forwardRef`) | ✅ Livré et vérifié |
| 8 | Identité visuelle distincte par faction (`/factions`, `/factions/[id]`) : police de titre, palette, motif de fond et style d'animation propres à chacune des 8 factions, système unique (`faction-themes.ts` + `FactionThemeScope`), respecte `prefers-reduced-motion`. Migre au passage `/factions/*` de `data/factions.ts` (statique) vers `GET /factions` (live). A aussi révélé et corrigé un crash (composant d'icône passé en prop brute d'un Server Component vers un Client Component — non sérialisable en RSC) | ✅ Livré et vérifié |
| 9 | CRUD Staff pour Factions et Départements (`/staff/factions`, `/staff/departements`, mêmes sous-routes que les Grades), même pattern (DTOs, guards, `forwardRef` où nécessaire). **Gap des 14/85 grades sans département résolu à 0/85** : typo de source corrigée (4 grades "scientifique" → département `recherche`), les 4 postes "Conseiller" du Conseil Oméga rattachés à leur département existant via correspondance `omegaTier`, Class-B/D/S rattachés à `securite` (déjà listé dans ses utilities), et nouveau département `Direction du Site` (O1) créé pour le Président du Conseil et les 2 grades de la branche "direction" | ✅ Livré et vérifié |
| 10 | Retrait des MTF comme structure RP active : pages `/factions/mtf`, nav, données statiques `mtfUnits`, index de recherche, `ApplicationType.MTF` (candidature web + Discord), catégorie et 5 grades "Forces Mobiles (MTF)" du catalogue de rôles Discord, lien mort du `/wiki` Discord. Chaque référence classée avant modification (ACTIVE vs HISTORIQUE/LORE) — le Lore (`narrative-core`, chronologie, actualités, wiki SCP) n'a pas été touché | ✅ Livré et vérifié |
| 11 | Chronologie officielle intégrée (1945-2027) dans `docs/LORE-BIBLE.md` et `data/timeline.ts` — réconciliation en cascade du lore existant qui dépendait des anciennes dates (Site-12, AEGIS, Directeur du Site, brèche Keter, Guerre interne O5→Oméga), correction d'une incohérence (SCP-████ "transféré depuis Site-19" alors que Site-19 est tombé en 2013), terminologie "MTF" → "FIM" partout dans le lore restant (narrative-core, SCP, CASSIE, seed) | ✅ Livré et vérifié |
| 12 | Modèle `Team` (générique, rattachée à `Department`, jamais un reskin de faction) : CRUD complet (même pattern que Grades/Factions/Départements), CMS staff (`/staff/teams`). Seed des 21 gabarits d'équipes déjà définis dans `site12.ts` (jamais migrés) **plus** les 5 escouades FIM du département Sécurité (Nu-7, Epsilon-11, Alpha-1, Beta-7, Zeta-9) — referme la boucle ouverte au Lot 10 : les FIM remplacent narrativement les MTF retirées, et existent maintenant réellement dans le CORE, pas seulement dans le lore | ✅ Livré et vérifié |
| 13 | Modèle `PlayerAssignment` (historique polymorphe FACTION/DEPARTMENT/TEAM, append-only) + `Player.teamId`/`teamInfo` (même pattern que `gradeId`/`factionId`). `POST /assignments` clôture automatiquement l'affectation ouverte précédente du même joueur pour le même type d'entité et synchronise l'état courant sur `Player` (`factionId`/`teamId` — `DEPARTMENT` n'a pas d'équivalent direct sur `Player`, l'historique reste alors la seule source). Toutes les routes réservées staff/admin (historique d'affectation = donnée sensible, pas de lecture publique comme Grades/Factions/Teams). API vérifiée en direct (création, clôture automatique à la 2e affectation, PATCH, DELETE) — **pas de CMS dédié cette fois** : choisir un joueur (recherche/autocomplete) est une vraie question de design UI, pas juste un formulaire plat comme les catalogues précédents ; backend livré, interface à construire en suivant | ✅ Backend livré et vérifié — UI à venir |
| 14 | Modèle `Sanction` : donne un corps à `Player.sanctions`, jusque-là un simple compteur entier mis à jour à la main sans aucun enregistrement derrière (type, motif, qui l'a prononcée, statut ACTIVE/EXPIREE/LEVEE, date de levée + par qui). Le compteur reste (lecture rapide `/profil` Discord et fiches joueur) mais devient un **total historique cumulatif** synchronisé automatiquement : incrémenté à la création, inchangé quand une sanction est levée (elle a bien eu lieu), décrémenté seulement à la suppression (correction d'une erreur de saisie, pas une clémence RP). Mêmes garde-fous que PlayerAssignment (staff/admin uniquement, DELETE réservé admin). API vérifiée en direct : création + incrément, levée + `liftedAt`/`liftedById` auto-remplis + compteur inchangé, suppression + décrément. Pas de CMS dédié non plus, même raison qu'au Lot 13 (picker joueur = vraie question de design UI) | ✅ Backend livré et vérifié — UI à venir |
| 15 | Fiche staff par joueur (`/staff/joueurs`, `/staff/joueurs/[username]`) : ferme la boucle ouverte aux Lots 13-14 en évitant complètement le problème du "picker" — au lieu d'un sélecteur de joueur générique, la sélection se fait par navigation (l'URL contient le pseudo). La fiche affiche le profil, l'historique de sanctions avec formulaire d'ajout et bouton "Lever", et l'historique d'affectations avec formulaire d'assignation (Faction/Département/Équipe) et bouton "Terminer". Nouvelle route `GET /players/:username/staff-id` (staff/admin) pour résoudre le `Player.id` interne depuis le pseudo, sans exposer cet id sur le profil public. Testé de bout en bout dans le vrai navigateur : création d'une sanction, levée, création d'une affectation, clôture — les quatre actions confirmées à l'écran avant nettoyage | ✅ Livré et vérifié |
| 16 | Connexion Discord conditionnée à l'appartenance au serveur REDLAKES : `DiscordService.isGuildMember()` (nouveau) interroge `GET /guilds/:id/members/:userId` avec le bot token, appelée à **chaque** connexion Discord (pas seulement à la création du compte — un membre qui quitte le serveur perd l'accès à la prochaine connexion). Dégradation gracieuse à trois niveaux : `DISCORD_BOT_TOKEN`/`DISCORD_GUILD_ID` absents → aucune vérification (comportement actuel inchangé tant que le bot n'est pas branché) ; API Discord indisponible ou erreur → fail-open (ne bloque jamais un membre légitime à cause d'un pépin technique) ; 404 → bloqué avec message dédié (`discord_not_member`) et lien direct vers l'invitation Discord. Vérifié directement (hors navigateur, un vrai flux OAuth Discord ne pouvant pas être piloté depuis ce environnement) : cas non-configuré laisse passer, cas configuré-mais-échec laisse aussi passer (fail-open) — comportement confirmé par script isolé, page d'erreur vérifiée dans le navigateur. **Mission a été proposé puis explicitement mis de côté par le staff pour l'instant** — non construit, non exposé dans la navigation | ✅ Livré et vérifié |
| 17 | `/departements`, `/departements/[id]` et `/departements/site-12` migrées de `site12.ts` (statique) vers l'API CORE — même traitement que Factions au Lot 8. Deux champs ajoutés à `Department` (`leadership: String[]`, `clearance: Int`) pour couvrir ce qui manquait encore ; `DepartmentsService.findOne` inclut désormais `grades`/`teams`, `findAll` expose leur compte (`_count`) pour la liste. Le Conseil Oméga et les "Branches Oméga" de la page `site-12` viennent aussi du live (`GET /grades?branch=omega`, départements eux-mêmes) plutôt que d'un tableau statique dupliqué. Chambres/expériences SCP (Département Scientifique uniquement) et la matrice d'accès décorative restent sourcées de `site12.ts` — aucun modèle Prisma ne les couvre, et ce sont explicitement des gabarits/exemples d'après le fichier source lui-même, pas des données fonctionnelles. Vérifié en direct sur les 3 pages (5 départements, équipes FIM incluses, 85 grades référencés) — **a mis en évidence un problème de qualité de données préexistant** dans le catalogue Grade déjà migré (Lots 1-2) : plusieurs grades ont `pay`/`quota` à `null` (Chef Réceptionniste, Technicien, Président du Conseil O1, entre autres) et quelques noms ont des coquilles ("Directeur Sécuriter", "Directeur Générale", "Géneral de Com.") — invisible jusqu'ici car ces pages affichaient encore des données statiques parfaites. Non corrigé dans ce lot (~85 fiches à revérifier proprement contre la source), signalé comme suivi à part | ✅ Livré et vérifié |
| 18 | Modèle `ScpObject` (nouveau, dédié — contenu trop structuré pour `LoreArticle` : classe, niveau de menace, confinement, incidents/tests/addendums en JSON, stats). Seed des 5 fiches déjà écrites dans `data/scp.ts`. CRUD complet + CMS staff (`/staff/scp`), même pattern que Team/Sanction, avec les tableaux imbriqués (incidents/tests/addendums) édités en JSON brut dans le formulaire plutôt qu'un éditeur de tableau dédié — proportionné pour un premier passage, pas une UI simplifiée abusive. `/wiki`, `/wiki/[id]` et le widget d'accueil `SCPPreview` migrés vers l'API (même traitement que Grades/Factions/Départements) ; `ArchivesClassified.tsx` et `lib/search.ts` **laissés statiques** volontairement — ils mélangent déjà SCP avec Lore/Événements/Personnages non migrés, les changer à moitié aurait produit un état plus incohérent que de les laisser tels quels jusqu'à ce que ces autres lots arrivent. `lib/search.ts` en particulier mélange aussi des données déjà live (Factions, Départements) sans les utiliser — la recherche globale tourne sur des instantanés figés depuis le Lot 8, gap préexistant non introduit ici, signalé comme suivi (la vraie solution est de brancher `GlobalSearch` sur `SearchService`, déjà en place côté API, plutôt que de maintenir un index statique dupliqué). Vérifié en direct : liste et fiche détail sur `/wiki`, widget d'accueil, CRUD complet testé (création via le vrai formulaire, PATCH/DELETE vérifiés, catalogue des 5 fiches d'origine confirmé intact après nettoyage) | ✅ Livré et vérifié |
| 19 | Modèle `Character` (nouveau, dédié) : personnages narratifs importants (directeurs, inspecteurs, figures du crime). `faction` reste un simple champ texte plutôt qu'une relation `Faction` — les valeurs existantes mélangent de vraies factions ("Fondation SCP") et des unités qui n'en sont pas ("FIM Nu-7"), forcer une relation aurait cassé la moitié des fiches. Seed des 5 fiches de `data/lore.ts` (à distinguer de `loreSections`/`gameEvents`, exportés par le même fichier mais non touchés ici — `loreSections` est déjà mort depuis que `/lore` tourne sur `LoreArticle`, `gameEvents` alimente encore `/evenements`, prochain sur la liste). CRUD + CMS staff (`/staff/personnages`) même pattern que d'habitude. `PersonnagesCatalog`/`PersonnageDetail` migrés vers l'API — restent des composants client (`usePlayerSession` pour le filtrage par habilitation), donc fetch en `useEffect` plutôt qu'en Server Component comme les pages Wiki/Départements. Vérifié en direct : liste avec gate d'habilitation correct pour un visiteur non identifié (seul Vincent Moretti, niveau 1, visible en clair), fiche détail complète pour ce personnage (biographie, citation, historique), fiche verrouillée correctement affichée pour un personnage de niveau 3, CRUD complet testé via l'API (création, PATCH, DELETE, catalogue des 5 fiches confirmé intact) | ✅ Livré et vérifié |
| 20 | Modèle `GameEvent` (nouveau, dédié) : brèches, invasions, guerres, crises XK, expériences ratées. `type` reste une `String` libre (`breach`/`invasion`/`guerre`/`crise-xk`/`experience`) plutôt qu'un enum Prisma — ces valeurs exactes (avec tiret) sont déjà les clés d'un dictionnaire d'affichage côté web ; un enum aurait forcé à les renommer en `CRISE_XK` etc. pour aucun gain. `date` en `DateTime` réel (pas juste une chaîne). Seed des 4 fiches de `gameEvents` dans `data/lore.ts` — dernier des trois exports narratifs de ce fichier à être migré (`loreSections` mort, `characters` fait au Lot 19). CRUD + CMS staff (`/staff/evenements`), `/evenements` et `/evenements/[id]` migrées en Server Components (pas de hook client ici, contrairement à Personnages). Vérifié en direct : liste triée par date décroissante avec les bons libellés de type, fiche détail complète (description/victimes/issue), CRUD complet testé via l'API, 4 fiches d'origine confirmées intactes. **Les trois tabs "narratifs" de `data/lore.ts` sont maintenant tous migrés** — `ArchivesClassified.tsx` et `lib/search.ts` restent volontairement statiques (déjà signalé aux Lots 18-19), c'est le seul morceau de dette restant sur ce fichier | ✅ Livré et vérifié |
| 21 | Modèle `NewsArticle` (nouveau, dédié) : `category` reste une `String` libre (même raisonnement que `GameEvent.type`), `date` en `DateTime` réel. Seed des 7 fiches de `data/news.ts`. CRUD + CMS staff (`/staff/actualites`). `/actualites` et `/actualites/[id]` migrées en Server Components ; le widget d'accueil `NewsSection.tsx` (déjà client, déjà en train de fusionner avec les Transmissions Discord live) bascule sa moitié statique vers `GET /news` sans toucher à la logique de fusion/tri existante. La page détail garde sa note "à connecter au CMS" — désormais vraie, l'article reste un simple résumé (`excerpt`), pas de corps de texte riche : hors périmètre de cette migration, à traiter si un jour la rédaction veut du contenu plus long. Vérifié en direct : liste triée par date avec bons libellés, fiche détail, widget d'accueil avec fusion Transmissions+Actualités confirmée, CRUD complet testé via l'API, 7 fiches d'origine confirmées intactes. **Cinq tabs migrés au total cette session : Départements, Wiki SCP, Personnages, Événements, Actualités.** Restent `/carte` (dernier tab statique identifié) et la dette déjà connue (`ArchivesClassified.tsx`/`lib/search.ts`, qualité du catalogue Grade, `Mission` en attente) | ✅ Livré et vérifié |
| 22 | Modèle `MapLocation` (nouveau, dédié) — dernier des tabs statiques identifiés. `type` et `faction` restent des `String` libres, mêmes raisonnements que `GameEvent.type`/`Character.faction` (dictionnaire d'affichage déjà existant, mélange de vraies factions et de groupes qui n'en sont pas). `x`/`y` en `Float` (coordonnées pourcentage 0-100, positionnement absolu déjà utilisé côté client, inchangé). Seed des 10 emplacements de `data/map.ts` — **corrige au passage la même incohérence "Site-12 fondé en 1962" déjà réglée plus tôt cette session** (`seed.ts`, `LoreArticle` "site-12-fondation") : devient "Construit entre 2017 et 2023, mis en service en 2024". CRUD + CMS staff (`/staff/carte`) ; pas d'éditeur de coordonnées cliquable, juste des champs X/Y numériques — proportionné, personne n'a demandé un éditeur visuel. `/carte` migrée en client component (elle l'était déjà, state de sélection interactif conservé tel quel) fetchant `GET /map` en `useEffect`. Vérifié en direct : rendu de la légende et des 10 marqueurs, sélection interactive fonctionnelle (clic sur un marqueur → panneau de détail live), correction "1962" confirmée à l'écran sur la fiche Site-12, CRUD complet testé via l'API, 10 emplacements d'origine confirmés intacts. **Les six tabs identifiés en début de session sont maintenant tous migrés** (Départements, Wiki SCP, Personnages, Événements, Actualités, Carte). Dette restante inchangée : `ArchivesClassified.tsx`/`lib/search.ts` toujours statiques (mélangent plusieurs sources désormais live sans les utiliser — vrai sujet de refactor GlobalSearch, pas une migration de plus), qualité du catalogue Grade (Lot 17), `Mission` en attente de validation | ✅ Livré et vérifié |
| 23 | Correction des coquilles du catalogue Grade signalées au Lot 17. Cause identifiée en creusant : `data/rp-grades.ts` est généré depuis un Excel (`scripts/generate-rp-catalog.py`, fichier source absent du repo — normalement dans les Téléchargements de l'utilisateur). Le staff a choisi de corriger à la main sans régénérer (option 2 sur 3 proposées) : les pay/quota manquants (26/85 grades) restent `null` — cause identifiée comme un vrai bug de réconciliation entre deux passes d'extraction du script (feuille paie vs. feuille tenue/équipement), non corrigeable sans le fichier source. 16 corrections de noms appliquées, chacune propagée à **toutes** ses occurrences (le champ `name` principal, mais aussi les tableaux `objectives`/`utilities` et le texte `description` d'autres grades qui listaient ces noms comme "missions" — repéré après un premier passage incomplet limité au seul champ `name`) : Directeur/Adjoint-Directeur/Com. Sécuriter → Sécurité, Directeur/Adjoint-Directeur Générale → Général, Général de Com. (accent), Médecin/Médecin Terrain/Médecin en Chef/Psychologue (accents + casse), Chef Réceptionniste (accent), Scientifique Aguerris (orthographe), Chercheur Expérimenteur (accent + suffixe), Superviseur d'Expérience (accent), Directeur du Site (casse). Les `id` (clés primaires, slugs d'URL) n'ont volontairement pas été touchés pour ne pas casser les liens existants — seul le `name` affiché change. Reseedé et vérifié en direct sur `/departements/securite` : "Directeur Sécurité" et "Superviseur d'Expérience" confirmés à l'écran, zéro fragment des anciennes coquilles restant dans la réponse API, total toujours 85 grades (aucun doublon introduit par l'upsert) | ✅ Livré et vérifié |
| 24 | Refonte du vécu de création de compte (Phase 1 de la refonte auth demandée par le nouveau document de vision) : Discord reste le moteur technique de connexion (`handleDiscordCallback`, session cookie inchangés — pas de nouveau système mot de passe, le projet n'a ni hashing ni infra d'envoi d'email) mais le parcours joueur suit désormais "lier Discord → créer son compte REDLAKES (personnage) → dossier", au lieu d'un atterrissage silencieux sur `/dashboard` avec un personnage "Civil" sans nom. Nouveau champ `User.onboardedAt` (nullable, backfillé à `now()` pour les 4 comptes existants — aucun ne se retrouve regaté), nouvelle route `POST /auth/onboarding/complete`, `onboarded` ajouté à `GET /auth/me`. Nouvelle page `/bienvenue` (réutilise les champs `rpFirstName`/`rpLastName` déjà connus de `CharacterSwitcher.tsx` et le `PATCH /players/me` existant) : bounce immédiat vers `/dashboard` si déjà onboardé, sinon formulaire de création du personnage avant d'y accéder. `RpLoginTerminal.tsx` recadré (bouton "Lier mon compte Discord", copie alignée sur le nouveau vécu) sans changer le mécanisme OAuth. Bot Discord non touché — son contrat passe entièrement par `/sync/discord/*` avec la clé API, jamais par `/auth/*`. Vérifié en direct : compte existant (backfillé) bounce sans interruption ; compte neuf (dev-login, même point de départ que Discord) voit le formulaire, la soumission écrit bien le nom RP et bascule `onboarded: true`, `/dashboard`/`/core` chargent normalement ensuite ; compte de test nettoyé | ✅ Livré et vérifié |
| 25 | Auth (suite Lot 24) : mot de passe modifiable à tout moment (`PATCH /auth/set-password` accepte désormais `currentPassword`, exigé et vérifié par `bcrypt.compare` uniquement si le compte en a déjà un — le cas première-définition depuis un compte Discord reste inchangé), section dashboard correspondante recadrée (`GradeEditor`-like toggle "Définir" / "Changer"). Suppression de personnage RP réservée STAFF/ADMIN — jamais en self-service pour un simple joueur, deux points d'entrée : fiche staff par joueur (`DELETE /players/:username/characters/:id`) et raccourci sur son propre tableau de bord pour un compte STAFF/ADMIN (`DELETE /players/me/characters/:id`, icône poubelle sur `CharacterSwitcher.tsx`, visible seulement si `role` STAFF/ADMIN). Si le personnage supprimé était l'actif, bascule automatique sur un autre personnage restant (sinon compte onboardé mais sans personnage actif) — `activeCharacterId` mis à jour avant le `delete`, pas seulement laissé au `onDelete: SetNull` de la FK. Vérifié en direct : suppression du personnage actif avec un autre restant (bascule confirmée), suppression du dernier personnage (liste vide, pas de crash), 403 confirmé pour un compte PLAYER sur les deux routes, comptes de test nettoyés | ✅ Livré et vérifié |
| 26 | REDLAKES CORE (le bureau plein écran `/core`, déjà livré sur cette branche mais jamais accessible autrement que par URL directe) relié au reste du site : bouton "Ouvrir REDLAKES CORE" sur le dashboard à côté du terminal intranet, icône dédiée dans `HeaderAuth.tsx` à côté de l'icône Intranet. Aucun changement à `/core` lui-même (gate d'authentification déjà correcte dans `app/core/layout.tsx`) — uniquement sa découvrabilité, répondant à une demande restée sans suite plus tôt dans le projet | ✅ Livré et vérifié |
| 27 | `Grade.clearanceLevel` (Int, 1-5) devient un vrai champ du catalogue au lieu d'être recalculé à la volée par regex sur le nom (`grade-clearance.ts`, conservé comme filet de sécurité pour un grade texte libre hors catalogue). Backfill initial par regex puis **corrigé** avec les vraies valeurs de `rp-grades.ts` (source Excel, 85 grades Fondation/Site-12) — 40 grades corrigés, écarts significatifs révélés (ex. "Adjoint-Directeur Se." recalé de 4 à 1, le regex confondait avec le grade homonyme de Direction). Les 57 grades des 7 autres factions (Police, AEGIS, Gouvernement, Crime, Chaos, Main du Serpent, GOC, Civil) n'ont pas d'équivalent Excel et gardent leur valeur regex, corrigible au cas par cas via le nouveau champ. CMS Grade (`GradeEditor.tsx`) et fiche staff (`/staff/grades`) exposent et éditent ce niveau ; `PlayersService.formatProfile` calcule un `clearanceLevel` résolu (lien catalogue en priorité, sinon regex) exposé sur `/players/me` ; `ReportsService.create` l'utilise pour classifier un rapport déposé (catalogue par `gradeId`, sinon recherche par nom, sinon regex) au lieu du seul regex. **A aussi révélé et corrigé un vrai bug de sécurité applicatif** : `TransmissionsFeedConnected.tsx` assignait un niveau 5 (accès total) à *tout* utilisateur authentifié, peu importe son grade réel, en contradiction directe avec la règle du spec ("clearance toujours vérifiée côté API") et avec son propre libellé ("Habilitation dérivée de votre grade") — corrigé pour lire le `clearanceLevel` réel via `/players/me`. Vérifié en direct : upsert idempotent (créer une relation A→B puis B→A mute la même ligne), validation `Min(1)/Max(5)` rejetée hors bornes, rapport déposé par un personnage dont le grade catalogue passe de 5 à 2 reclassé immédiatement en 2, page Transmissions affichant "Niveau 1" et 1/1 transmission accessible pour un personnage réellement niveau 1 (plus jamais 5 par défaut) | ✅ Livré et vérifié |
| 28 | Modèle `FactionRelation` (diplomatie inter-factions, dernier morceau documenté du backlog "17+") : statut Alliée/Neutre/Tensions/Hostile + note libre entre deux factions. Relation non dirigée — `factionAId`/`factionBId` canonicalisés par ordre d'id côté service (`canonicalPair`) pour qu'une paire ne soit jamais dupliquée A↔B, contrainte `@@unique` en sécurité supplémentaire ; "définir une relation" fait un `upsert`, jamais besoin de savoir si elle existe déjà côté appelant. CRUD staff (`POST/PATCH` STAFF+ADMIN avec `MinRank COORDINATEUR_GENERAL`, `DELETE` ADMIN seul — même partition que Factions), nouvelle section sur `/staff/factions/[id]` (`FactionRelationsPanel.tsx`). Lecture publique (`GET /faction-relations`, `GET /faction-relations/faction/:id`) affichée sur la fiche faction publique (`/factions/[id]`) sous forme de badges colorés cliquables. Vérifié en direct : les deux sens de la paire retournent bien la même ligne avec la faction "en face" résolue correctement, auto-relation rejetée (400), rendu confirmé aussi bien côté staff (formulaire + liste) que côté page publique, comptes et relations de test nettoyés | ✅ Livré et vérifié |
| 29 | Modèle `ClassifiedDocument` (documents classifiés) — même patron que `LoreArticle` (le plus proche existant : auteur, statut DRAFT/PUBLISHED/ARCHIVED, restriction par département, tags, historique de révisions), avec `factionId` optionnel (faction concernée) et `attachments` (liste d'URLs) en plus. Scope volontairement proportionné pour un premier passage : les liens vers SCP/événements/missions/autres documents décrits dans le spec ne sont pas encore modélisés en relations (pas de `Mission` construite, et une jointure many-to-many par type de lien aurait démultiplié la portée sans besoin exprimé) — seul le lien vers une `Faction` existe. CRUD staff (`POST/PATCH` STAFF+ADMIN `MinRank COORDINATEUR_GENERAL`, `DELETE` ADMIN seul, `GET .../by-id/:id` et `GET .../:id/revisions` staff), CMS complet (`/staff/documents`, `.../nouveau`, `.../[id]` avec `ClassifiedDocumentRevisionHistory`). Lecture publique gated exactement comme Lore (`GET /classified-documents` et `GET /classified-documents/:slug` sous `OptionalAuthGuard`, filtrées via `department-visibility.ts` déjà en place — un accès restreint et une fiche inexistante renvoient la même 404, aucune fuite d'existence) : `/documents` (catalogue) et `/documents/[slug]` (lecture, écran "Accès refusé" dédié si hors département). Lien ajouté à la nav principale (`Header.tsx`, section Systèmes) et au tableau de bord staff. Pas d'intégration à `SearchService` dans ce lot (la recherche globale tourne déjà sur un index statique obsolète, gap préexistant signalé au Lot 18 — brancher un contenu de plus dessus n'aurait fait qu'ajouter à la dette plutôt que la résorber). Vérifié en direct de bout en bout : création en DRAFT invisible publiquement, publication, un joueur du département autorisé voit le document dans la liste et la fiche (200), un joueur d'un autre département reçoit l'écran "Accès refusé" (404 côté API), une édition crée bien une révision de l'état précédent (2 révisions après 2 modifications, contenu et auteur corrects), suppression refusée à un compte STAFF non-ADMIN (403), toutes les données de test nettoyées | ✅ Livré et vérifié |
| 30 | **REDLAKES CORE (le bureau `/core`) mis en pause sur décision explicite de l'utilisateur** ("je ne pense pas le continuer") — retrait des deux liens ajoutés au Lot 26 (bouton dashboard, icône `HeaderAuth`), plus jamais découvrable depuis la navigation normale. Code intégralement conservé (`app/core/`, `components/core/*`, la gate d'authentification de `app/core/layout.tsx`) — reste atteignable par URL directe si besoin, rien supprimé, décision réversible. Les fiches Faction (`/factions/[id]`) restent la direction retenue pour "chaque faction a son propre espace" | ✅ Livré et vérifié |
| 31 | Fiche faction publique (`/factions/[id]`) enrichie en véritable vitrine — décision explicite de l'utilisateur : "quand tu clique sur la Faction ça t'envoie une interface qui parle d'avantages de la faction et des choses intéressantes" plutôt qu'une mini-appli séparée façon CORE (redondant avec CORE, écarté). Nouveau bandeau "en un coup d'œil" (4 tuiles : membres actifs, départements, meilleur salaire — dérivé des vrais `Grade.pay` de la faction via `departmentRef`, alliées/hostiles — dérivé de `FactionRelation` déjà livré au Lot 28), section Membres (roster des personnages actifs de la faction, `Player.factionId`, nouvelle route publique `GET /factions/:slug/members`, cliquable vers la fiche joueur), section Documents classifiés (filtrée depuis le catalogue `ClassifiedDocument.factionId` déjà posé au Lot 29, jusque-là orphelin). Aucune donnée inventée — uniquement de l'agrégation sur ce qui existe déjà. Vérifié en direct sur `/factions/fondation` avec les vraies données de production : 3 membres actifs affichés correctement (dont le compte réel de l'utilisateur, "Adams Wolf — Président du Conseil (O1)"), meilleur salaire "20 000 $ (Vice-Président — Sécurité (O2))" correct, tuile relations à "0 / 0" en l'absence de relation réelle définie pour cette faction | ✅ Livré et vérifié |
| 32 | Édition en place ("cliquer sur un texte pour le modifier") déployée sur l'ensemble des pages publiques de contenu — décision explicite de l'utilisateur ("pour la totalité du site je dois pouvoir modifier à ma guise juste en cliquant sur un texte"). Trois briques réutilisables (`components/staff/`) : `EditModeProvider`/`EditModeToggle` (bouton flottant global, monté dans `RouteChrome.tsx`, visible uniquement STAFF/ADMIN via `usePlayerSession`), `EditableText` (clic → petit formulaire en place → `PATCH` direct sur l'endpoint déjà existant du type de contenu, aucune nouvelle route API créée), `OptionalSection` (garde les sections normalement masquées quand le champ est vide visibles en mode édition, sinon impossible d'ajouter un premier contenu à un champ jamais rempli). Câblé sur les champs texte principaux (nom/titre, description/extrait, corps long) de Factions, Grades, Départements, Wiki SCP, Lore, Personnages, Événements, Actualités, Carte et Documents classifiés — dix types de contenu, tous les endpoints `PATCH` déjà livrés aux lots précédents, aucune nouvelle route créée. Restent volontairement hors périmètre : champs tableaux/relations/énumérations (tags, zones d'accès, restriction par département...), qui continuent de passer par les formulaires CMS dédiés déjà existants — l'édition en place cible le texte, pas la reconfiguration de structure ; et les Équipes, qui n'ont pas de fiche publique du tout (uniquement `/staff/teams`). **A révélé et corrigé en repassant dessus un vrai bug de conception** : la première version passait une fonction `render` en prop à `EditableText` pour le rendu Markdown — cassait au chargement (500, "Functions cannot be passed directly to Client Components") sur toutes les pages Server Component (Factions, Grades, Wiki, Événements, Actualités), une fonction ne pouvant pas traverser la frontière Server → Client Component (React interdit les props non sérialisables) — corrigé en remplaçant par des props booléennes sérialisables (`markdown`, `paragraphs`, `wrapperClassName`, `scpRefsSlug`), exactement le motif déjà établi par `DiscordMarkdown` lui-même (son prop `scpRefsSlug` existe pour cette même raison, documentée dans son propre fichier). **A aussi révélé un second gap** en cours de route : `router.refresh()` seul ne suffit pas partout — plusieurs pages chargent leurs données côté client via `useEffect` (Carte, Personnages, Lore, Documents), et `router.refresh()` ne relance que le rendu Server Component, pas ce fetch-là ; corrigé en donnant à `EditableText` un état local (`savedValue`) qui reflète immédiatement la sauvegarde, indépendamment du mécanisme de refetch du parent. Vérifié en direct de bout en bout après les deux corrections : édition réussie sur une fiche Grade (Server Component — nouvelle description ajoutée à un champ vide, section "Description" apparue uniquement en mode édition, persistée en base, confirmée par `GET /grades/:slug` direct) et sur une fiche Carte (Client Component — description modifiée et visible immédiatement sans rechargement, la preuve concrète que le correctif `savedValue` fonctionne) ; bouton "Mode édition" absent du DOM pour un compte PLAYER (vérifié par inspection directe, pas juste visuellement) ; tentative de `PATCH` direct par ce même compte PLAYER rejetée en 403 côté API — la protection réelle reste le rôle vérifié côté serveur, l'UI n'est qu'un raccourci. Toutes les données de test nettoyées | ✅ Livré et vérifié |
| 33 | Dette signalée depuis les Lots 18-22 refermée : la recherche globale (`GlobalSearch.tsx`) mélangeait un index statique figé (`lib/search.ts`, 7 tableaux `data/*.ts` jamais synchronisés avec la base) à ses résultats API en cas de succès — pas seulement en repli sur panne. **Deux vrais bugs**, pas juste de la fraîcheur de données : (1) tout contenu créé/édité/supprimé via le CMS après le seed initial restait invisible ou fantôme côté statique ; (2) plus grave, l'index statique n'a aucune notion de restriction par département — contrairement à `SearchService` côté API qui applique déjà `filterByDepartment` (lore/scp/personnages/événements) — donc un contenu classifié pouvait apparaître dans les résultats de recherche d'un visiteur qui n'a pas le droit de le voir, dès que l'API renvoyait moins de résultats que le statique sur la même requête. Corrigé : `GlobalSearch.tsx` n'utilise plus que `GET /search`, plus de fusion ; échec réel de l'API affiché explicitement ("Recherche indisponible") plutôt que masqué par un repli silencieux. Même correctif appliqué à OPHIS (`lib/ophis.ts`), qui avait exactement le même motif à un endroit différent (repli sur le statique dès que l'API renvoyait 0 résultat, pas seulement en cas de panne réelle) — désormais un vrai `catch`, jamais un 0 résultat légitime. `lib/search.ts` conservé (encore utilisé par OPHIS comme vrai filet de secours en cas de panne API, cas légitime). Au passage, `ClassifiedDocument` (Lot 29) — jusque-là invisible des deux côtés, absent de `SearchService.fallbackSearch` comme du statique — ajouté au SQL de repli, même patron que Lore (statut `PUBLISHED` + `filterByDepartment`). Vérifié en direct : recherche anonyme sur un document classifié ne renvoie que sa version publique non restreinte, un joueur du bon département voit aussi la version restreinte, widget de recherche du site testé en conditions réelles dans le navigateur (résultat affiché, lien correct), toutes les données de test nettoyées | ✅ Livré et vérifié |
| 34 | Fiche faction (suite Lot 31), deux ajouts demandés en se mettant à la place d'un joueur qui hésite entre les factions plutôt que de continuer à empiler des stats abstraites. **(a)** Section "Ce que tu fais dans cette faction" (rôles/catégories, déjà livrée au Lot 22 mais reléguée tout en bas de la fiche) remontée juste après le bandeau de stats — c'est la question qu'un joueur se pose en premier, elle ne devrait pas être noyée sous description/historique/objectifs/relations. **(b)** Nouveau lien optionnel `GameEvent.factionId` (nullable, `onDelete: SetNull`, même patron que `ClassifiedDocument.factionId` au Lot 29) pour montrer "ce qui bouge en ce moment" plutôt qu'une fiche figée — sélecteur faction ajouté à `GameEventEditor.tsx` (même select inline que `DepartmentEditor`/`ClassifiedDocumentEditor`), nouvelle route publique `GET /factions/:slug/events` (6 plus récents) filtrée par habilitation département du demandeur exactement comme `EventsService.findAll` (`filterByDepartment`), affichée en section "Activité récente". Pour resoudre correctement cette habilitation, la fiche faction doit désormais transmettre le cookie de session du visiteur au fetch serveur — jusque-là aucune des fonctions de `faction-api.ts` ne le faisait (fetch anonyme systématique) ; comme ce fichier est aussi importé par des Client Components (`FactionHero.tsx`, `FactionRelationsPanel.tsx`), le fetch avec `cookies()` de `next/headers` a été gardé local à la page (`factions/[id]/page.tsx`, même principe que `getScpObject` dans `wiki/[id]/page.tsx`) plutôt que placé dans le fichier partagé, qui aurait cassé leur bundle client. **A aussi révélé un vrai cycle de modules** en essayant d'abord d'injecter `PlayersService` dans `FactionsController` pour cette résolution de département : `FactionsModule → PlayersModule → SyncModule → FactionsModule` (Sync importe déjà Factions). Résolu en dupliquant la petite requête Prisma (`resolveDepartmentId`, 6 lignes) directement dans `FactionsService` plutôt qu'en forçant un `forwardRef` en cascade sur des modules pré-existants pour un besoin aussi ponctuel. Vérifié en direct sur `/factions/fondation` avec deux évènements de test liés à la faction : le public visible par tout visiteur anonyme, le restreint à un département invisible pour ce même visiteur et pour un compte d'un autre département, visible dès que le personnage actif appartient au bon département ; éditeur CMS confirmé pré-sélectionnant la bonne faction à l'ouverture et sauvegardant correctement (`PATCH` vérifié directement, révision archivée) ; toutes les données de test (évènements, joueur, compte) nettoyées | ✅ Livré et vérifié |
| 35 *(rétroactif)* | **Documentation a posteriori** — ce lot et le suivant consignent un chantier déjà livré (branche `feature/redlakes-core-os`, code jamais retouché ici) mais jamais écrit lot par lot, comme la ligne "35+" le signalait elle-même. Reconstitué par lecture du code et de l'historique git, pas par une nouvelle vérification en direct — les statuts ci-dessous décrivent ce que le code fait, non une re-vérification de cette session. **Construction initiale de REDLAKES CORE** (commit `d151eb4`, 22/08/2026) : bureau plein écran par-dessus le reste du site (`app/core/layout.tsx`, gate identique au reste du site — même `/auth/me` que partout, aucun mécanisme d'auth spécifique à CORE), gestionnaire de fenêtres entièrement fait main (`CoreShellProvider.tsx` + `CoreWindow.tsx`, Pointer Events bruts pour le drag, pas de librairie, cascade automatique à l'ouverture, pas de redimensionnement libre — juste minimiser/maximiser) sans aucune persistance (state React en mémoire, tout se perd au rechargement), écran de démarrage purement visuel (`CoreBootSequence.tsx`, une seule fois par onglet via `sessionStorage`, respecte `prefers-reduced-motion`). Dix des onze applications livrées dès ce commit (Profil, Notifications, Rapports, Carte, Missions, Personnel, Équipes, Communications, OPHIS, plus un stub Documents non fonctionnel) — chacune consomme un vrai endpoint déjà existant (`/platform/notifications/me`, `/reports/me`, `/map`, `/missions/me`, `/players`, `/teams`, `/map`), aucune donnée simulée, seul le libellé change par faction (`FACTION_LABEL_OVERRIDES` dans `lib/core/app-registry.ts`, ex. Police voit "Main courante"/"Dispatch" là où la Fondation voit "Rapports"/"Personnel") — même moteur et mêmes onze apps pour toutes les factions, jamais une app ajoutée ou retirée selon la faction. Nouveau modèle `CoreMessage` (fil partagé par faction, `factionId` nullable = canal Civil commun) pour l'app Communications, `authorLabel` figé au moment de l'envoi (nom RP + grade, ou pseudo Discord/Minecraft en repli) plutôt que résolu à la lecture — cohérent avec le motif déjà établi par `editedByLabel` sur les révisions. Au passage : refonte de `GET /auth/me` en un seul appel consolidé (faction/grade/département/équipe/rang staff), et premières passes de contenu sur Wiki/Personnages/Événements/Chronologie/Factions/Grades/Carte en dehors de CORE lui-même. *(Un commentaire de code obsolète relevé en écrivant cette entrée — `app-registry.ts` disait encore "même 9 apps partout" alors que l'app Messagerie du Lot 36 en fait onze depuis longtemps — corrigé au passage.)* | ✅ Livré (précédemment) |
| 36 *(rétroactif)* | **Messagerie privée + explorateur de documents réel** (commit `6649b29`, 26/08/2026, même branche). Nouveau modèle `CoreDirectMessage` (expéditeur/destinataire par `User`, `readAt` nullable) et module API `core-dm` (`GET /core-dm/conversations`, `GET /core-dm/unread-count`, `GET /core-dm/with/:username`, `POST /core-dm`) pour la 11e app, Messagerie — vraie messagerie 1-à-1 par pseudo Minecraft, auto-envoi bloqué, accusés de lecture réels (ouvrir un fil marque les messages de l'autre partie comme lus), mais sans temps réel : ni WebSocket ni polling, un simple fetch à l'ouverture et après envoi. L'app Documents (stub non fonctionnel au Lot 35) réécrite en vrai explorateur : interroge `/lore`, `/events`, `/scp` (les mêmes routes déjà filtrées par département côté API, voir Lots antérieurs sur `filterByDepartment`) et ne garde que les fiches avec `restrictedDepartmentIds` non vide, classées en trois dossiers (Lore/Événements/Dossiers SCP classifiés) — **volontairement le même mécanisme que la page publique `/archives`** (`ArchivesClassified.tsx`), pas un système de classification propre à CORE ; le modèle dédié `ClassifiedDocument` (Lot 29) reste une fonctionnalité séparée, non consommée ici. Ce même chantier a aussi ajouté le parcours d'onboarding joueur (personnage RP à la création de compte), un quiz d'intégration, et le lien de la carte interactive — hors périmètre CORE, non détaillés ici. Le bureau CORE au complet (onze apps, ~2 600 lignes sur 33 fichiers entre web et API) a ensuite été relié à la navigation (Lot 26) puis retiré sur décision explicite de l'utilisateur (Lot 30) — code toujours intact, atteignable par URL directe | ✅ Livré (précédemment) |
| 37 | **Audit de sécurité de `/archives` (`ArchivesClassified.tsx`)** — vérifié suite au doute soulevé en préparant le Lot 36 : cette page affiche les fiches Lore/Événements/SCP portant une `restrictedDepartmentIds` non vide en les filtrant *côté client* sur ce seul critère, sans revérifier elle-même le département de l'utilisateur — exactement la forme du bug corrigé au Lot 33 (GlobalSearch/OPHIS). Différence cette fois : **pas un bug**. Les trois routes qu'elle appelle (`GET /lore`, `GET /events`, `GET /scp`) résolvent chacune le vrai département de l'appelant via `OptionalAuthGuard` + `PlayersService.getDepartmentId` et appliquent déjà `filterByDepartment` côté API avant de répondre (même patron que `EventsService.findAll`) — le filtre client de la page ne fait que resélectionner "restreint" parmi des résultats déjà correctement autorisés, redondant mais inoffensif. **Corrige au passage une autre inexactitude de la ligne "en attente"** : elle qualifiait `ArchivesClassified.tsx` de "statique" au même titre que `lib/search.ts` — faux, la page ne contient aucune donnée figée, seul `lib/search.ts` (catalogue de repli d'OPHIS, cas légitime déjà noté au Lot 33) l'est réellement. Aucun changement de code nécessaire, seule la documentation était erronée | ✅ Vérifié — aucun correctif requis |
| 38 | **Nouveau `docs/REDLAKES-DESIGN-SYSTEM.md`** — le catalogue de contenu était devenu assez riche pour justifier de formaliser le design déjà en place plutôt que d'empiler encore des systèmes (repositionnement discuté avec l'utilisateur suite à un rapport complémentaire externe orienté design/UX/immersion). Trois audits menés en parallèle avant d'écrire quoi que ce soit : palette et typographie réelles (`globals.css`, `faction-themes.ts`), relations réelles entre entités en base, mobile/accessibilité. **Résultat notable du deuxième audit : l'idée d'un composant générique "entités associées" partout sur le site ne tient pas** — seule `Faction` a un vrai graphe de relations (départements, membres, événements, documents, diplomatie) ; `ScpObject`, `Character`, `LoreArticle`, `NewsArticle` et `MapLocation` n'ont **aucune** clé étrangère réelle vers autre chose, seulement des champs texte qui y ressemblent (`Character.faction`, `MapLocation.faction`) et que le code documente déjà comme non fiables — un tel composant aurait été vide sur 4 fiches sur 6, ou aurait tenté quelqu'un de le brancher sur ces champs non fiables. Seul vrai gap trouvé et corrigé : `GameEvent.factionId` existait mais n'était affiché nulle part — ajouté sur `/evenements/[id]` (badge cliquable vers la faction, même patron visuel que le badge déjà existant sur `/documents/[slug]`). Le troisième audit a révélé et fait corriger de vrais bugs, pas des choix de goût : le menu principal (`Header.tsx`) était inaccessible au clavier sur tout le site (ouverture à la souris uniquement) — corrigé (ouverture au focus, fermeture Échap, `aria-expanded`, vérifié en direct au clavier réel) ; deux animations en boucle infinie (`.pulse-glow`, posé sur *tous* les boutons primaires via `Button.tsx`, et `.map-radar-ring`) n'avaient pas de garde `prefers-reduced-motion` malgré le motif déjà appliqué ailleurs dans le même fichier — corrigé ; la grille de stats de la fiche Wiki SCP (3 colonnes fixes, aucune classe responsive dans tout le fichier) cassait sur mobile — corrigé ; plusieurs formulaires staff (`grid-cols-[1fr_2fr_auto]` etc. sur la fiche joueur et le panneau de relations diplomatiques) écrasaient les champs sur mobile au lieu de simplement déborder — corrigé. Au passage : le statut "archivé" changeait de couleur selon la page (jaune pour les documents, gris pour les rapports de faction) — unifié sur une échelle à 5 niveaux cohérente avec `AlertLevelBadge` (gris=inactif, jaune=en attente, vert=actif, orange=avertissement, rouge=danger) ; et deux définitions de `CLEARANCE_LABELS` avec un texte différent (`lib/clearance.ts` vs `GradeEditor.tsx`, ce dernier utilisant un libellé "Conseil O5" propre à la Fondation sur un champ qui s'applique à toutes les factions) — le doublon supprimé, `GradeEditor` importe désormais la source unique. Vérifié en direct : navigation clavier réelle du menu (Tab ouvre, Tab entre dans les sous-liens, Échap ferme), badge faction affiché et cliquable sur un événement de test, grille Wiki SCP empilée correctement à 375px, toutes les données de test nettoyées | ✅ Livré et vérifié |
| 39 | Focus Wiki SCP, deux corrections concrètes signalées/trouvées en creusant, pas des choix de goût. **(a)** Menu déroulant "Trier" du catalogue (`WikiCatalog.tsx`) illisible — signalé par capture d'écran par l'utilisateur, options grisées sur fond clair alors que le reste du site est sombre. Cause réelle : `color-scheme` n'était défini nulle part sur le site, donc les listes natives (`<select>`) retombaient sur le thème clair par défaut du navigateur au lieu du thème sombre du système — `color-scheme: dark` ajouté sur `:root`, plus une règle explicite `select option { background/color }` en filet de sécurité pour les moteurs qui ignorent la couleur héritée dans le popup natif. **A aussi révélé un vrai problème d'environnement** en vérifiant : le cache Turbopack (`.next/`) servait un bundle CSS obsolète malgré la modification du fichier source et un redémarrage complet du serveur — la recompilation ne repartait que de zéro après suppression manuelle de `.next/`, sinon `color-scheme` restait absent du CSS compilé indéfiniment (à garder en tête pour tout futur changement CSS qui semble "ne pas s'appliquer"). **(b)** CMS `ScpEditor.tsx` : les incidents/tests/addendums (déjà signalés "raccourci proportionné" au Lot 18) s'éditaient en JSON brut dans une zone de texte — remplacé par un vrai éditeur structuré (ajout/suppression/édition ligne par ligne, un champ par propriété), zéro changement d'API ou de forme de donnée stockée. Au passage, les 3 grilles à colonnes fixes de ce même formulaire (déjà repérées au Lot 38 comme dette restante) rendues responsives. Vérifié en direct sur `SCP-002` (données réelles de production) : les valeurs existantes se chargent correctement dans le nouvel éditeur, ajout d'une ligne d'incident et sauvegarde confirmés par lecture directe de l'API et sur la fiche publique, suppression de ligne confirmée, **une erreur de manipulation pendant la vérification a temporairement écrasé le résumé du vrai incident de SCP-002** (ciblage du mauvais champ dans le script de test, pas un bug du composant) — repérée immédiatement, données et historique de révisions restaurés à l'identique avant de continuer, compte de test nettoyé | ✅ Livré et vérifié |
| 40 | Première vraie relation many-to-many entre types de contenu — le spec d'origine prévoyait des documents liés "aux SCP/événements/missions/autres documents", explicitement reporté au Lot 29 faute de besoin exprimé ; confirmé absent par l'audit du Lot 38 (seule `Faction` avait un vrai graphe de relations). Deux relations implicites Prisma ajoutées : `ClassifiedDocument ↔ GameEvent` et `ClassifiedDocument ↔ ScpObject` (tables de jointure `_DocumentEvents`/`_DocumentScpObjects`, cascade des deux côtés). Édition volontairement centralisée côté `ClassifiedDocumentEditor.tsx` uniquement (`linkedEventIds`/`linkedScpIds`, deux nouveaux sélecteurs `EventMultiSelect`/`ScpMultiSelect` avec filtre texte + liste déroulante, même patron que `DepartmentMultiSelect`) — les fiches Événement et SCP n'ont qu'un affichage en lecture seule du lien inverse, pas d'éditeur symétrique, pour éviter de dupliquer la logique. **Le point sensible était la sécurité, pas la fonctionnalité** : un document public ne doit jamais laisser fuiter le titre d'un événement ou d'un SCP restreint via ce nouveau lien — `ClassifiedDocumentsService.findBySlug` filtre désormais `linkedEvents`/`linkedScpObjects` par département (et exige `status: APPROVED` pour un SCP) avant de répondre, et symétriquement `EventsService.findOne`/`ScpService.findOne` ne renvoient que les documents `PUBLISHED` et visibles du département du demandeur dans `linkedDocuments` — jamais la liste brute. Vérifié en direct avec un vrai événement restreint à un département : visible et cochable dans l'éditeur staff, absent de la réponse publique anonyme du document qui le lie, présent dès que le visiteur appartient au bon département ; suppression en cascade confirmée (document supprimé → plus aucune trace côté événement/SCP) ; toutes les données de test nettoyées | ✅ Livré et vérifié |
| 41 | Push Discord — l'API annonce désormais elle-même la publication de contenu. Trois déclencheurs ajoutés, choisis par l'utilisateur : document classifié publié (`ClassifiedDocumentsService`, uniquement au passage DRAFT/ARCHIVED→PUBLISHED, jamais republié à chaque édition ultérieure), nouvel événement RP créé, nouvelle actualité créée — chacun via une nouvelle méthode dédiée sur `DiscordService` (`notifyDocumentPublished`/`notifyEventPublished`/`notifyNewsPublished`). **Mécanisme livré en deux temps** : première version via un webhook (même patron que les 5 notifications existantes, `DiscordService.postWebhook`) — puis l'utilisateur a fait remarquer qu'un vrai bot tourne déjà (`DISCORD_BOT_TOKEN` déjà configuré pour `isGuildMember`/`syncMemberProfile` dans ce même fichier), donc aucune raison de faire créer un webhook en plus. Remplacé par un envoi direct `POST https://discord.com/api/v10/channels/{id}/messages` avec `Authorization: Bot <token>` (nouvelle méthode privée `postToContentChannel`, même dégradation gracieuse que le reste du fichier), piloté par une nouvelle variable `DISCORD_CHANNEL_CONTENT` — peut réutiliser tel quel l'ID du salon `CHANNEL_ANNOUNCES` déjà configuré côté bot (`apps/discord-bot/.env`), aucun webhook à créer dans Discord. Recherche préalable dans une autre session ("Ajustements bot Discord") pour éviter de dupliquer un salon/webhook déjà existant — confirmé qu'aucun salon ni webhook dédié au contenu du site n'existait encore, mais que `CHANNEL_ANNOUNCES` (annonces de grade) est le candidat naturel à réutiliser. **Point de sécurité vérifié explicitement** : un document ou événement restreint à un département ne doit jamais être annoncé publiquement sur Discord — l'annonce est conditionnée à `restrictedDepartmentIds.length === 0`, aussi bien à la création qu'à la transition vers PUBLISHED. Import de `SyncModule` dans `ClassifiedDocumentsModule`/`EventsModule`/`NewsModule` sans créer de cycle cette fois (contrairement au Lot 34) — vérifié avant de coder que rien dans l'arbre de dépendances de `SyncModule` ne réimporte ces trois modules. Vérifié en direct à trois reprises. D'abord sans `DISCORD_CHANNEL_CONTENT` configuré : création d'un événement, d'une actualité et d'un document publié via l'API réelle — aucune régression, no-op silencieux confirmé (zéro appel réseau tenté, donc aucun risque de poster sur un vrai salon pendant ces tests), serveur démarré sans exception de dépendance circulaire. Puis **`DISCORD_CHANNEL_CONTENT` renseigné avec le vrai salon `annonces-fondation`** (l'utilisateur a communiqué les IDs des trois salons déjà configurés côté bot ; les deux autres — Transmissions Site-12 et bienvenue — écartés comme non pertinents pour cet usage) : après confirmation explicite de l'utilisateur avant d'envoyer quoi que ce soit de visible sur son vrai serveur, un événement de test a été créé via l'API réelle, le message est bien apparu dans le vrai salon Discord (`⚠️ Nouvel événement — TEST DIAGNOSTIC...`, confirmé par lecture directe du salon via le token du bot), puis supprimé du salon Discord lui-même (pas seulement de la base) avant de nettoyer l'événement et le compte de test. Le mécanisme est donc réellement opérationnel en production, pas seulement compilé | ✅ Livré et vérifié |
| 42+ | `Mission`+`MissionParticipant` (en attente de validation staff), puis Wallet/Transaction, RpEvent. Dette identifiée mais non traitée : ~12 formulaires CMS staff restants à grille fixe non responsive (hors `ScpEditor` et `staff/joueurs`, déjà corrigés), modale de recherche globale sans sémantique de dialogue/piège de focus/label accessible, contraste des petits libellés gris jamais vérifié formellement (voir `docs/REDLAKES-DESIGN-SYSTEM.md`). Escouades AIT non créées — aucune donnée existante à migrer, noms/composition à valider avant toute création (voir §9bis de la Lore Bible : ne pas inventer de canon sans validation). Suite de la refonte auth (phases 2+ du nouveau document de vision : RBAC/ABAC dérivé du Grade, espaces de branche dynamiques, hiérarchie générée depuis les données) — non commencée | À venir |
